import styled from "styled-components";

export namespace DetailHeaderStyle{
    export const Container = styled.div`
        width: 100%;
        min-height: 5vh;
        height: 5vh;
    `

    export namespace Button{
        export const Container = styled.div`
            cursor: pointer;
        `

        export const Image = styled.div`
            
        `
    }
}